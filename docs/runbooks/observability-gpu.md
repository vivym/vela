# GPU telemetry or temperature alert

Check `up{job="nvidia-smi"}`, exporter logs, `nvidia-smi`, driver health,
temperature, power and ECC/XID state on the affected host. Keep the four
protected hosts read-only. A GPU node is drained only through the normal Vela
maintenance authority after model workloads are accounted for.
